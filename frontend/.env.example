# Base URL of the Express REST API (includes version prefix)
NEXT_PUBLIC_API_URL=http://localhost:4000/api/v1

# Public base URL of the site (used for metadata / share links)
NEXT_PUBLIC_SITE_URL=http://localhost:3000
